# my-r-code-repo
Replication code for spatial econometrics analysis
# Replication Code for My Project
This repo contains R code for SDPD models.
Installation
install.packages(c("splm", "plm"))
 Usage
source("Replication_Code.R")
